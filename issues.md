# issues with the extension

## STILL ISSUES
- looks like, on some tweets — particularly the ones that are particularly long — the rewordings seem to get cut off
- on quote-tweets, it places the "inflammation score" and the "show original" button on the quote-tweet, even if it's rewording the quoted tweet
- occasionally takes a really long time to load the inflammation score, and then the "see original" button. the "loading" animation makes this better, but if (a) the loading animation occured _earlier_ that'd be better, and (b) if things just loaded _faster_ that'd be better.
  - in particular: it should show the inflammation score _before_ it shows the "see original" button.
  - in particular: it should show the loading animation _before_ it shows the "see original" button, if the loading animation is needed at all.

## FIXED

### reliability

- doesn't add a score on some tweets
- doesn't give an "see original" button on some tweets
- sometimes takes a really long time to load the inflammation score, and then the "see original" button

### random

- is costly/expensive
